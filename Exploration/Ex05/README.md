# AIFFEL Campus Online Code Peer Review Templete


- 코더 : 장기훈
- 리뷰어 : 강지수


# PRT(Peer Review Template)

- [ ] **1. 주어진 문제를 해결하는 완성된 코드가 제출되었나요?**
    - 영화 코퍼스 다운로드부터 Okt 명사 추출, Word2Vec 학습, TF-IDF 대표 단어 선정, WEAT 계산 및 시각화까지 전체 파이프라인이 단계별로 구성되어 있습니다.
    - Word2Vec은 62,908개의 학습 문장과 16,782개의 Vocabulary로 정상 학습되었으며, `most_similar()` 결과에서도 `영화`에 `무성영화`, `사극`, `극영화`, `액션`에 `하드코어`, `블록버스터`, `무협`, `어드벤처` 등이 나타나 영화 코퍼스의 문맥을 의미 있게 학습한 것을 확인했습니다.
    - 다만 장르 파일을 지정하는 `GENRE_FILES`의 파일명이 실제 압축 해제된 파일명과 일치하지 않습니다. 실제 파일은 `synopsis_family.txt`, `synopsis_horror.txt`, `synopsis_documentary.txt` 등인데 설정에서는 `synopsis_가족.txt`, `synopsis_공포(스릴러).txt`, `synopsis_다큐멘터리.txt` 등을 사용하고 있습니다.
    - 이로 인해 SF를 제외한 장르의 문서 수가 0으로 처리되었고, 장르별 target 단어 집합 역시 대부분 빈 리스트가 되었습니다. 최종 WEAT matrix도 대각선을 제외한 값이 대부분 `NaN`이므로 현재 출력된 Heatmap은 장르별 편향성을 해석하기 어려운 상태입니다.
    - 또한 원본 데이터에는 21개 장르 파일이 존재하지만 현재 `GENRE_FILES`에는 12개 장르만 포함되어 있어, 전체 장르를 대상으로 한 분석까지 확장하면 프로젝트 요구사항을 더욱 충실하게 만족할 수 있을 것 같습니다.


- [x] **2. 전체 코드에서 가장 핵심적이거나 가장 복잡하고 이해하기 어려운 부분에 작성된 주석 또는 doc string을 보고 해당 코드가 잘 이해되었나요?**
    - 노트북이 환경 준비 → 데이터 다운로드 → 형태소 분석 → Word2Vec → TF-IDF → WEAT → 시각화 순으로 구분되어 있어 전체 분석 흐름을 따라가기 쉬웠습니다.
    - `load_lines()`, `clean_text()`, `tokenize_nouns()`, `get_tfidf_top_words()`처럼 전처리와 대표어 추출 과정이 함수로 나뉘어 있어 각 코드의 역할을 이해하기 쉬웠습니다.
    - 예술영화/일반영화 단어의 중복을 제거하고, 다시 장르 target에서 attribute와 겹치는 단어를 제외한 점도 WEAT 단어 집합의 중복을 고려하려는 의도가 명확했습니다.
    - WEAT 계산 역시 `cos_sim()` → `association()` → `weat_effect_size()` 순서로 나누어 구현되어 수식이 코드에서 어떤 단계로 계산되는지 따라가기 좋았습니다.
    - 다만 WEAT에서 장르를 target으로, 예술/일반영화를 attribute로 정의한 이유와 최종 score의 양수·음수가 무엇을 의미하는지를 Markdown으로 조금 더 설명해주면 분석 결과를 이해하기 더 쉬울 것 같습니다.


- [x] **3. 에러가 난 부분을 디버깅하여 문제를 해결한 기록을 남겼거나 새로운 시도 또는 추가 실험을 수행해봤나요?**
    - 데이터 압축 해제 후 실제 파일 목록을 출력하여 데이터 구조를 직접 확인하는 과정이 포함되어 있었고, 파일이 없을 때 `[경고] 파일 없음`을 출력하도록 구현하여 데이터 로딩 문제를 확인할 수 있도록 한 점이 좋았습니다.
    - 기본적인 장르 간 WEAT matrix 외에도 `compute_single_axis_bias()`를 별도로 구현해 각 장르와 예술/일반영화 축 사이의 association을 막대그래프로 표현하려는 추가 시도가 있었습니다.
    - 한글 폰트 문제를 해결하기 위해 Matplotlib font cache를 초기화하고 다시 로드하려는 디버깅 시도도 확인했습니다.
    - 다만 파일명 불일치 경고가 실제 출력에 나타났음에도 이후 `GENRE_FILES`를 수정하지 않아, 핵심 분석 결과까지 `NaN`이 전파되었습니다. 특히 필수 데이터 파일의 경우 빈 리스트를 반환하고 계속 진행하기보다 `FileNotFoundError`를 발생시키거나, 모든 파일의 존재 여부를 먼저 검사한 뒤 분석을 시작하면 오류를 더 빨리 발견할 수 있을 것 같습니다.
    - 한글 폰트 역시 최종 시각화에서 `NanumGothic not found` 경고가 계속 발생하므로, 폰트 등록 여부를 확인한 뒤 Heatmap을 다시 생성하면 제출 결과의 가독성이 좋아질 것 같습니다.


- [ ] **4. 회고를 잘 작성했나요?**
    - 프로젝트 목표와 각 단계의 설명은 잘 정리되어 있으나, 프로젝트를 수행하며 새롭게 알게 된 점이나 예상과 달랐던 부분, 아쉬웠던 점을 정리한 별도의 회고는 확인하지 못했습니다.


- [ ] **5. 코드가 간결하고 효율적인가요?**
    - 데이터 로드, 전처리, TF-IDF, WEAT 계산을 각각 함수로 분리한 점은 재사용성과 가독성 측면에서 좋았습니다.
    - 특히 `compute_weat_matrix()`와 `compute_single_axis_bias()`를 별도의 함수로 구현하여 분석 로직과 출력 코드를 분리한 점이 좋았습니다.
    - Heatmap 코드가 두 번 반복되고, 장르별 bar plot 코드도 두 번 반복되어 있습니다. 동일한 시각화 셀은 한 번만 유지하면 노트북이 더 간결해질 것 같습니다.
    - `MIN_TOKEN_LEN=2`는 노이즈 제거에는 도움이 되지만 `집`, `삶`, `꿈`처럼 의미 있는 한 글자 한국어 명사도 함께 제거할 수 있으므로, 길이만으로 일괄 제거하기보다 분석 목적에 맞는 불용어 기준을 검토해보면 좋겠습니다.


# 회고(참고 링크 및 코드 개선)

기훈님의 노트북은 프로젝트 전체 과정을 환경 설정부터 시각화까지 단계적으로 구성하고, 각 기능을 함수로 나누어 구현한 덕분에 분석 흐름을 이해하기 쉬웠습니다. 특히 Word2Vec의 `most_similar()` 결과에서는 영화 코퍼스의 문맥적 관계가 비교적 잘 나타났고, 기본 WEAT matrix 외에 장르별 단일 축 편향 점수를 별도로 시각화하려고 한 점도 배울 부분이 있었습니다.
